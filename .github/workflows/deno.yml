name: deploy-int

on:
  workflow_dispatch:
    inputs:
      build_id:
        required: true
        type: string

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy build to target
        run: echo ${{ inputs.build_id }}
        
